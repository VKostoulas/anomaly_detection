# Anomaly Detection With AutoEncoders And Perceptual Loss

- /architectures contains all the custom network architectures.
- /functions contains all the functions that are used everywhere in the project.
